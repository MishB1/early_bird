/*
Template for Utility Functions

- Add utility functions here (e.g., date formatting, validators).
- Example:
  static String formatDate(DateTime date) {
    return '${date.day}/${date.month}/${date.year}';
  }
*/

class DateUtils {
  // Add your utility functions here
  static String formatDate(DateTime date) {
    return '${date.day}/${date.month}/${date.year}';
  }
}