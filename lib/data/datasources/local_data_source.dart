/*
Template for Local Data Sources

- Define local data sources here (e.g., SQLite, SharedPreferences).
- Example:
import 'package:school_attendance/data/models/student_model.dart';

  class LocalDataSource {
    Future<StudentModel> getStudentByFingerprint(String fingerprint) async {
      // Fetch student from local database
    }
  }
*/

